//while loop:
const colors = ["yellow", "blue", "red", "orange"];
let i = 0;
while (i < colors.length) {
  console.log("print:", colors[i]);
  i++; //nooit dit weghalen, anders blijft de loop lopen en crasht de computer!!!
};
