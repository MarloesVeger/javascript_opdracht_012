const colors = ["yellow", "blue", "red", "orange"];
colors.forEach(element => console.log(element));

//1. while loop: 6 regels;
//For loop: 4 regels;
//2. forEach: 2 regels;
// forEach: Het is overzichtelijker en makkelijker;
//4. Je kunt een array method niet gebruiken op een object;
